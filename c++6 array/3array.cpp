#include <iostream>

using namespace std;

int main()
{
    int arr[3];    // With specifying the size of the array:
    arr[0] = 1, arr[1] = 2, arr[2] = 3;
    cout<<"The element at index "<<0<<" is "<<arr[0];
    return 0;
}