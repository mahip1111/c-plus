#include <iostream>
using namespace std;
int sum(int a, int b){
    return a+b;
}
int main() {int a,b;
    cout<<"Enter two numbers: ";
    cin>>a>>b;
    cout<<"The sum of "<<a<<" and "<<b<<" is: "<<sum(a,b);
    return 0;
}

// the function in c and c++ are almost same difference pta karna par abhi ke liya mane kar chalo ki same hi tarah se donon kam karta hai c aur c++ main.